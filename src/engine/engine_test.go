package engine

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func Test_Engine(t *testing.T) {
	Convey("Given a working set of dependencies", t, func() {

		Convey("The engine should not fail", func() {

		})

	})
}
